fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.13.5
pydantic-settings==2.5.2
python-dotenv==1.0.1
python-multipart==0.0.9
aiofiles==24.1.0
httpx==0.28.1
google-genai==2.24.0
pypdf==6.19.0
pandas==3.0.6
openpyxl==3.1.5

# --- Batch processing ---
# Celery 5.4 is the last line that still starts cleanly on Windows with
# the threads/solo pools. Prefork needs fork(), which Windows lacks;
# see worker/celery_app.py. Pinned rather than floated for that reason.
celery==5.4.0
# The Redis transport for Celery, plus the client the broker probe uses.
redis==5.2.1
